# ── Web / API ────────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# ── Data / ORM ───────────────────────────────────────────────
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# ── Queue / cache ────────────────────────────────────────────
rq>=2.0,<3.0
redis>=5.0,<6.0

# ── Storage (Cloudflare R2 via S3 API) ───────────────────────
boto3>=1.34,<2.0

# ── Media indexing ───────────────────────────────────────────
opencv-python-headless>=4.9,<5.0
scenedetect>=0.6.4,<0.7
numpy>=1.26,<3.0
librosa>=0.10,<0.11
soundfile>=0.12,<0.14

# ── Twelve Labs SDK (method surface verified against docs.twelvelabs.io) ──
twelvelabs>=0.4

# ── Generation (Phase 1): captions + caption rendering ──────
anthropic>=0.39
Pillow>=10.2,<12.0

# ── Utilities ────────────────────────────────────────────────
tenacity>=8.2,<10.0
httpx>=0.27,<1.0
